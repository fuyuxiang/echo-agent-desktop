diff --git a/crates/codegen/xai-grok-shell/src/session/helpers/session_summary.rs b/crates/codegen/xai-grok-shell/src/session/helpers/session_summary.rs
index 78e12ade..0a4c28b4 100644
--- a/crates/codegen/xai-grok-shell/src/session/helpers/session_summary.rs
+++ b/crates/codegen/xai-grok-shell/src/session/helpers/session_summary.rs
@@ -30,6 +30,83 @@ pub(crate) fn checkpoints_reached(turns: usize) -> usize {
 /// little shorter — fine for a safety bound.
 const TITLE_MAX_BYTES: usize = 80;
 
+/// XML-like tags emitted by some reasoning-capable OpenAI-compatible models
+/// inside `assistant.content`. They are internal reasoning, never a title.
+const INTERNAL_REASONING_TAGS: [&str; 4] = ["think", "thinking", "reasoning", "analysis"];
+
+fn is_tag_boundary(byte: Option<u8>) -> bool {
+    byte.is_some_and(|b| b == b'>' || b.is_ascii_whitespace())
+}
+
+fn find_reasoning_tag(
+    lower: &str,
+    tag: &str,
+    closing: bool,
+    from: usize,
+) -> Option<(usize, Option<usize>)> {
+    let needle = if closing {
+        format!("</{tag}")
+    } else {
+        format!("<{tag}")
+    };
+    let mut offset = from;
+    while let Some(relative) = lower[offset..].find(&needle) {
+        let start = offset + relative;
+        let after_name = start + needle.len();
+        if is_tag_boundary(lower.as_bytes().get(after_name).copied()) {
+            let end = lower[after_name..]
+                .find('>')
+                .map(|relative_end| after_name + relative_end + 1);
+            return Some((start, end));
+        }
+        offset = start + 1;
+    }
+    None
+}
+
+fn find_first_reasoning_tag(lower: &str) -> Option<(usize, &'static str, Option<usize>)> {
+    INTERNAL_REASONING_TAGS
+        .into_iter()
+        .filter_map(|tag| {
+            find_reasoning_tag(lower, tag, false, 0).map(|(start, end)| (start, tag, end))
+        })
+        .min_by_key(|(start, _, _)| *start)
+}
+
+/// Whether a title still contains an opening or closing internal-reasoning tag.
+/// Used by persisted-session display as a defense for titles written by older
+/// builds before generation-time cleanup existed.
+pub(crate) fn contains_internal_reasoning_markup(raw: &str) -> bool {
+    let lower = raw.to_ascii_lowercase();
+    INTERNAL_REASONING_TAGS.into_iter().any(|tag| {
+        find_reasoning_tag(&lower, tag, false, 0).is_some()
+            || find_reasoning_tag(&lower, tag, true, 0).is_some()
+    })
+}
+
+fn strip_internal_reasoning_blocks(raw: &str) -> String {
+    let mut output = raw.to_string();
+    loop {
+        let lower = output.to_ascii_lowercase();
+        let Some((start, tag, open_end)) = find_first_reasoning_tag(&lower) else {
+            break;
+        };
+        let Some(open_end) = open_end else {
+            output.truncate(start);
+            break;
+        };
+        let lower = output.to_ascii_lowercase();
+        let Some((_, Some(close_end))) = find_reasoning_tag(&lower, tag, true, open_end) else {
+            // A truncated/unclosed reasoning block is not safe title text.
+            // Keep anything before it, but discard the block and its tail.
+            output.truncate(start);
+            break;
+        };
+        output.replace_range(start..close_end, "");
+    }
+    output
+}
+
 /// Durable title-refresh checkpoint watermark under `{session_dir}/`: the number
 /// of [`TITLE_REFRESH_TURNS`] checkpoints already consumed. Written on every
 /// completed attempt (success or failure) so the freeze survives resume,
@@ -195,7 +272,10 @@ Just generate the session_title and nothing else"#,
                 && let Some(tool_call) = a.tool_calls.first()
                 && let Ok(result) = serde_json::from_str::<SessionTitle>(&tool_call.arguments)
             {
-                return result.session_title;
+                let title = clean_title_text(&result.session_title);
+                if !title.is_empty() {
+                    return title;
+                }
             }
             tracing::debug!(
                 model = %model,
@@ -234,7 +314,13 @@ pub(crate) fn title_refresh_instruction(tag: &str) -> String {
 /// (whitespace collapse, stray label/quote stripping) plus the
 /// [`TITLE_MAX_BYTES`] cap.
 pub(crate) fn clean_title_text(raw: &str) -> String {
-    let mut out = crate::session::helpers::session_recap::clean_recap_text(raw);
+    // Strip reasoning before applying the byte cap. Otherwise a long `<think>`
+    // prefix is truncated first and the actual title after `</think>` is lost.
+    let without_reasoning = strip_internal_reasoning_blocks(raw);
+    if contains_internal_reasoning_markup(&without_reasoning) {
+        return String::new();
+    }
+    let mut out = crate::session::helpers::session_recap::clean_recap_text(&without_reasoning);
     if out.len() > TITLE_MAX_BYTES {
         let cut = floor_char_boundary(&out, TITLE_MAX_BYTES);
         out.truncate(cut);
@@ -325,6 +411,29 @@ mod tests {
         assert!(capped.len() <= super::TITLE_MAX_BYTES);
     }
 
+    #[test]
+    fn clean_title_removes_reasoning_before_applying_cap() {
+        let raw = format!(
+            "<think>{}</think>\n简短问候与任务确认",
+            "private reasoning ".repeat(20)
+        );
+        assert_eq!(clean_title_text(&raw), "简短问候与任务确认");
+    }
+
+    #[test]
+    fn clean_title_rejects_unclosed_or_reasoning_only_output() {
+        assert!(clean_title_text("<think>private reasoning").is_empty());
+        assert!(clean_title_text("<analysis>private</analysis>").is_empty());
+    }
+
+    #[test]
+    fn clean_title_handles_case_and_tag_attributes() {
+        assert_eq!(
+            clean_title_text("<THINK data-mode=\"deep\">private</THINK>\nFix login redirect"),
+            "Fix login redirect"
+        );
+    }
+
     #[test]
     fn title_source_text_caps_oversized_input() {
         let big = "word ".repeat(10_000);
diff --git a/crates/codegen/xai-grok-shell/src/session/persistence.rs b/crates/codegen/xai-grok-shell/src/session/persistence.rs
index 06c5ac8d..f76437a3 100644
--- a/crates/codegen/xai-grok-shell/src/session/persistence.rs
+++ b/crates/codegen/xai-grok-shell/src/session/persistence.rs
@@ -1021,11 +1021,30 @@ impl Summary {
 
     /// Preferred display title: `generated_title` if non-empty, else `session_summary`.
     pub fn display_title(&self) -> &str {
-        self.generated_title
+        let generated = self
+            .generated_title
             .as_deref()
             .map(|t| t.trim())
-            .filter(|t| !t.is_empty())
-            .unwrap_or(&self.session_summary)
+            .filter(|t| !t.is_empty());
+        if let Some(title) = generated
+            && (self.title_is_manual
+                || !crate::session::helpers::session_summary::contains_internal_reasoning_markup(
+                    title,
+                ))
+        {
+            return title;
+        }
+
+        let fallback = self.session_summary.trim();
+        if !self.title_is_manual
+            && crate::session::helpers::session_summary::contains_internal_reasoning_markup(
+                fallback,
+            )
+        {
+            ""
+        } else {
+            fallback
+        }
     }
 
     /// [`Self::display_title`] as an `Option`, `None` when blank.
diff --git a/crates/codegen/xai-grok-shell/src/session/persistence_generated_title_tests.rs b/crates/codegen/xai-grok-shell/src/session/persistence_generated_title_tests.rs
index 99c72dae..e8cacf4a 100644
--- a/crates/codegen/xai-grok-shell/src/session/persistence_generated_title_tests.rs
+++ b/crates/codegen/xai-grok-shell/src/session/persistence_generated_title_tests.rs
@@ -74,6 +74,39 @@ fn summary_includes_generated_title_when_set() {
     assert!(json.contains("Fix K8s deployment"));
 }
 
+#[test]
+fn auto_title_with_internal_reasoning_markup_is_not_displayed() {
+    let mut summary = Summary::new(
+        &Info {
+            id: acp::SessionId::new("test"),
+            cwd: "/tmp".into(),
+        },
+        default_model_id(),
+    )
+    .unwrap();
+    let leaked = "<think>private chain of thought</think>Final title";
+    summary.generated_title = Some(leaked.into());
+    summary.session_summary = leaked.into();
+
+    assert_eq!(summary.display_title(), "");
+}
+
+#[test]
+fn manual_title_with_angle_bracket_text_is_preserved() {
+    let mut summary = Summary::new(
+        &Info {
+            id: acp::SessionId::new("test"),
+            cwd: "/tmp".into(),
+        },
+        default_model_id(),
+    )
+    .unwrap();
+    summary.generated_title = Some("Document <think> XML tag".into());
+    summary.title_is_manual = true;
+
+    assert_eq!(summary.display_title(), "Document <think> XML tag");
+}
+
 #[test]
 fn summary_deserializes_with_all_fields_present() {
     let json = r#"{
diff --git a/crates/codegen/xai-grok-shell/src/session/storage/summary_write.rs b/crates/codegen/xai-grok-shell/src/session/storage/summary_write.rs
index 9527f2eb..441d5055 100644
--- a/crates/codegen/xai-grok-shell/src/session/storage/summary_write.rs
+++ b/crates/codegen/xai-grok-shell/src/session/storage/summary_write.rs
@@ -231,7 +231,12 @@ impl Summary {
     /// title too.
     fn set_title(&mut self, title: &str) {
         self.generated_title = Some(title.to_owned());
-        if self.session_summary.is_empty() {
+        if self.session_summary.is_empty()
+            || (!self.title_is_manual
+                && crate::session::helpers::session_summary::contains_internal_reasoning_markup(
+                    &self.session_summary,
+                ))
+        {
             self.session_summary = title.to_owned();
         }
     }
