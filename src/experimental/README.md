# 未接入生产执行链路的适配器

这些模块保留用于独立验证，生产入口不得引用。存在实现或单元测试不代表桌面已经支持对应能力。

- `sandbox-init.ts`：实验性第三方 Sandbox API 适配。安装包不会自动对 Runtime 启用操作系统沙箱。
- `container-executor.ts`：Docker 执行适配原型，当前工具命令不经过该执行器。
- `oidc-auth.ts`：通用 OIDC/PKCE 原型，当前组织认证使用已接入的原生协议。

正式接入时必须补端到端权限、取消、退出、凭据生命周期测试，再迁移到对应功能模块。
