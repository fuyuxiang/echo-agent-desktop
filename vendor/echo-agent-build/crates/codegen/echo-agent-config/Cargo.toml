[package]
license = "Apache-2.0"
name = "echo-agent-config"
version = "0.1.0"
edition.workspace = true
description = "Shared config loading for EchoAgent — echo_agent_home, effective config (requirements > user > managed), TOML merge"

[dependencies]
base64 = { workspace = true }
blake3 = { workspace = true }
dunce = { workspace = true }
# Shared signed deployment-config envelope contract with the cli-chat-proxy signer.
echo-agent-chat-proxy-types = { path = "../../../prod/mc/echo-agent-chat-proxy-types" }
ring = { workspace = true }
semver = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
shellexpand = "3.1"
thiserror = { workspace = true }
toml = { workspace = true }
tracing = { workspace = true }
urlencoding = { workspace = true }
which = { workspace = true }
echo-agent-home = { workspace = true }
echo-agent-tty-utils = { workspace = true }
echo-agent-version = { workspace = true }

# core-foundation: read macOS MDM managed preferences (`ai.x.echo-agent`) for the
# admin-deployed requirements layer.
[target.'cfg(target_os = "macos")'.dependencies]
core-foundation = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[features]

[lints]
workspace = true