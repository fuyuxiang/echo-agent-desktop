# @echo-agent-official/echo-agent-linux-x64

Packaging template for the EchoAgent pager binary on Linux x64.

This package is maintained for the repository's release pipeline and is not currently published to npm. EchoAgent Desktop bundles the runtime directly.
