[package]
edition.workspace = true
license = "Apache-2.0"
name = "echo-agent-prompt-queue"
version = "0.1.0"
description = "Shared prompt-queue wire types for echo-agent-runtime and echo-agent-pager"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[lints]
workspace = true
