# Project Instructions

- Future commit messages in this repository must not use conventional commit prefixes such as `feat:`, `fix:`, `docs:`, `chore:`, or scoped variants like `feat(chat):`.
- Use a plain, concise commit subject that describes the change directly.
